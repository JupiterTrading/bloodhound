import { ExplorerPage } from "@/components/explorer/ExplorerPage";

export const metadata = {
  title: "Explorer — BLOODHOUND",
  description: "Search wallets, tokens, transactions, and programs on Solana.",
};

export default function Page() {
  return <ExplorerPage />;
}
